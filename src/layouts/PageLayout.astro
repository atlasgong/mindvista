---
import BaseLayout from "@layouts/BaseLayout.astro";

interface Props {
    pageTitle: string;
    description?: string;
}

const { pageTitle, description } = Astro.props;
const suffix = false;
---

<BaseLayout pageTitle={pageTitle} description={description} suffix={suffix}>
    <div class="">
        <slot />
    </div>
</BaseLayout>